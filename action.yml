name: 'AltWalker Action'
description: 'This action installs AltWalker.'
branding:
  icon: 'check'
  color: 'blue'
runs:
  using: "composite"
  steps:
    - run: ${{ github.action_path }}/install_graphwalker.sh
      shell: bash
    - run: ${{ github.action_path }}/install_altwalker.sh
      shell: bash
